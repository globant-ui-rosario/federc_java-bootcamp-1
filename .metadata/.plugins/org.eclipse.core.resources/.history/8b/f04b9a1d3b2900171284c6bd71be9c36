package keyPoint7;

import java.util.ArrayList;

public class CashMethod implements PaymentMethod {// I must apply Iterator pattern, but i can't figure it out how to do it (relying on the design pattern documentation)
	

	@Override
	public void pay(ArrayList<ShoppingCartLine> shoppingCartLineCollection) {
		System.out.println("paid with cash --> 90% discount of the most expensive item");
		int sum = 0;
		for (int index = 0; index < shoppingCartLineCollection.size() ; index++){
			// save the value of the most expensive item in a variable
			//delete most expensive item
			// then, calculate the total as: sum += (price*quant)+ variable that contains the value*0.1;	
		}
		
		System.out.println("you get: "+ sum);
	}

}
